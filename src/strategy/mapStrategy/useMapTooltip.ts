import { TooltipOption } from "echarts/types/dist/shared";
import { RenderPropOptions } from "../../models/propOptionModel";

export function buildMapTooltip(props: RenderPropOptions) {
  return {} as TooltipOption;
}
